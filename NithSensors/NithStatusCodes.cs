namespace NeeqDMIs.NithSensors
{
    public enum NithStatusCodes
    {
        OPR,    // Operative
        ERR,    // Error (generic)
        NCA,    // Need calibration
        ICA,    // In calibration
        NaC     // Not a status code
    }
}
